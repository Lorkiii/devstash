# Server-only configuration. Fill .env.local privately; never paste values into chat.
AUTH_URL=http://localhost:3000
AUTH_SECRET=
AUTH_GOOGLE_CLIENT_ID=
AUTH_GOOGLE_CLIENT_SECRET=

# Neon pooled URL for runtime and separate direct URL for approved CLI operations.
DATABASE_URL=
DIRECT_URL=
